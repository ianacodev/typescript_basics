/*****************************
 * Tuple Types for Arrays.
 * Tuple allows suggest to typescript some kind of data struct inside an
 * array made of different types.
 * If api doesnt always return an array of objects.
 *****************************/
// will need to conform to signature order.
let fakeApiResponseData: [number, string, boolean];
fakeApiResponseData = [10, 'hello world', false];
